---
---

--- Before Day-4 ---
I already knew Modern Application Development, System Commands.
--- 

## Day-4 Checklist

- [x] I know the 5 core HTTP methods (GET, POST, PUT, PATCH, DELETE) and what each is used for
- [x] I can read a status code and know what went wrong — e.g., 401 vs. 403 vs. 404 vs. 500
- [x] I can open Chrome DevTools Network tab, find a request, and inspect its headers, payload, and response
- [x] I can copy a browser request as a cURL command and run it in the terminal
- [x] I can change the `User-Agent` in the browser and see the change in the Network tab
- [x] I can use `curl` to make a GET request with query parameters and a POST request with a JSON body
- [x] I have a running FastAPI app with at least two endpoints (`GET /health` and `POST /echo`)
- [x] I can test my API using the Swagger UI at `/docs` and via `curl` from the terminal

--- After Day-4 ---
Beyond the core checklist, today's session provided crucial insights into modern developer workflows. I learned how to use the 'Network' tab in Chrome DevTools to not just inspect requests, but to replicate them by generating cURL commands, which can then be converted into clean Python code using tools like curlconverter.com. I also gained a better understanding of how APIs operate under the hood by building a functional FastAPI server and testing it with custom route logic. Most importantly, the instructors emphasized that in 2026, proficiency is less about memorizing syntax and more about mastering concepts, effective debugging, and leveraging AI assistants to handle boilerplate tasks efficiently.
---

--- Feedback (Suggestions for the TDS Team) ---
The session was highly practical, but it felt quite intense for someone from a non-programming background. I suggest adding a small 'pre-session' resource list for future days that covers basic terminal navigation or specific Python libraries ahead of time, as it would help bridge the knowledge gap before diving into advanced topics like async programming or API development. The real-time coding demos were excellent, but slowing down slightly during the setup phase of the FastAPI server would make it easier to follow along without feeling overwhelmed.
---

---
---

You can write your personal notes here; they will not be parsed and are for your own reference.
