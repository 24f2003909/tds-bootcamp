---
---

--- Before Day-2 ---
I already knew Modern App Development, System Commands.
--- 

## Day-2 Checklist

- [x] I understand what `PATH` is and why commands like `python` work without full paths
- [x] I can navigate the filesystem without clicking — using `cd`, `ls`, and `pwd` only
- [x] I can read, search, and inspect files using `cat`, `head`, `tail`, `grep`, and `wc`
- [x] I can edit a file using `nano` (open, edit, save, exit)
- [x] I understand pipes (`|`) and redirection (`>`, `>>`, `2>`) and can chain commands
- [x] I can set an environment variable in `.bashrc` and apply it with `source ~/.bashrc`
- [x] I know the difference between `export VAR=value` (available to child processes) and just `VAR=value` (shell-local)

--- After Day-2 ---
Beyond the core checklist, I gained a practical understanding of how environment variables function in the context of parent-child process relationships, specifically using the export command. I also learned how to use the source command to refresh configurations without needing to restart the shell and gained experience customizing the terminal startup sequence by injecting scripts into .bashrc, such as automating system information displays with tools like fastfetch.
---

--- Feedback (Suggestions for the TDS Team) ---
The session was informative, though I would suggest incorporating more visual demonstrations—similar to the whiteboard explanations used on the first day—to help simplify complex conceptual topics like path variable resolution and shell hierarchy. Additionally, providing clear, step-by-step documentation for installation processes would significantly help students who might experience connectivity or setup delays during live coding portions.
---

---
---

You can write your personal notes here; they will not be parsed and are for your own reference.
