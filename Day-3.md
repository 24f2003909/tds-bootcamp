---
---

--- Before Day-3 ---
I already knew Modern Application Development, System Commands.
--- 

## Day-3 Checklist

- [x] I can run a Python script using `uv run script.py` without setting up a local virtual environment
- [x] I know where the temporary virtual environment is created when running the `uv add --script script.py pandas` command followed by `uv run script.py`
- [x] I can create a new Python project using `uv init` and understand what `pyproject.toml` is used for
- [x] I can add a dependency (e.g., `requests`) using `uv add` and see it reflected in the lockfile
- [x] I can create a traditional virtual environment using `uv venv` and know when to use it
- [x] I understand why installing packages globally with `pip install` is a bad habit
- [x] I can open a project in VS Code, select the correct Python interpreter, and run code from the integrated terminal
- [x] I know the difference between a `.py` script and a `.ipynb` notebook, and when to use each

--- After Day-3 ---
Beyond the core workflow items, today's session provided a solid foundation in essential developer utilities. I learned how to effectively use VS Code navigation shortcuts like Ctrl + B and Ctrl + P to streamline my coding environment, and mastered the basics of Markdown for clear project documentation. I also gained clarity on why isolating dependencies with virtual environments is critical for project stability and how UV significantly simplifies this process by handling environment creation and dependency locking through the pyproject.toml and uv.lock files. Finally, understanding the role of .gitignore in protecting sensitive API keys was a key takeaway for maintaining secure coding practices.
---

--- Feedback (Suggestions for the TDS Team) ---
The session was highly practical and well-structured, particularly the transition from Linux/WSL fundamentals to modern Python tooling. The hands-on demonstrations using UV were especially valuable, as they demystified project management compared to legacy methods. I would suggest adding a brief resource list or a 'cheat sheet' for the VS Code shortcuts covered, as it would help us retain the commands more easily. Overall, the pacing was perfect for those of us trying to bridge the gap between foundational programming and professional-grade development workflows.
---

---
---

You can write your personal notes here; they will not be parsed and are for your own reference.
